The Main file needed is test.py because the other files are not rewuired but there is no issue if you download that.Download the roboticarm.ino and test.py and in the pythn file specify the COM Port.
